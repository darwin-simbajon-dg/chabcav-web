import React from "react";

const Header: React.FC = () => {
    return (
        <header style={{background: "url('/images/chabacano1.jpg') no-repeat center center",
            backgroundSize: "cover", // Ensures the image covers the entire area
            height: "150px",}}>
        </header>
    );

};

export default Header;
